# ai_incident_model

Train an incident severity machine learning model and use a Streamlit UI template to collect incident inputs, predict severity, and show the result in a small dashboard.

## Run

```bash
pip install -r requirements.txt
python -m unittest tests_test_incident_model.py
# python train.py
streamlit run app.py
```
